// Minimal ambient type declarations for the backend Database
// This stub unblocks TypeScript builds until generated types are available.
export type Json = string | number | boolean | null | { [key: string]: Json | undefined } | Json[];
export interface Database {}
